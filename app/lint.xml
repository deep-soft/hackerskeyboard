<?xml version="1.0" encoding="utf-8"?>
<lint>
    <issue id="NamespaceTypo" severity="ignore" />
    <!-- only problem was that we used android: shortcut for our own
	 namespace. Not a typo, we are deliberately defining a new
	 namespace because we are defining new tags
    -->
</lint>
